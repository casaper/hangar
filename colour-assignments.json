{
  "4": "red",
  "_": "Written by `hangar colours change <clone> <colour>`. Keys are clone indices; values are palette names from app/src/palette.ts. A clone that is not listed takes PALETTE[(N-1) % length]."
}
