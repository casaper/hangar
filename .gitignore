# The fleet root IS a git repository (branch `main`, no remote) tracking only the fleet map and
# the shell helpers. These entries are what keep the clones and the secrets out of it, so they
# are load-bearing, not insurance. Do not remove them.
.env.shared
.remember/
clone_0*/
