# Per-clone values ONLY. Secrets shared by every clone live one level up in
# %HANGAR%/.env.fixture-shared, loaded by this clone's .envrc.private before this file
# (so anything set here still overrides the shared value).
#
# Ports must differ per clone: two clones sharing a dev server means a test run in one
# silently verifies the other one's code. Check with `hangar ports`.
#
# Generated by `hangar add-clone` from the clone index (2); repair a drifted
# value with `hangar doctor --fix` rather than by hand.

FIXTURE_REPO_ROOT='%HANGAR%/wt-002'

FIXTURE_API_PORT=3137
FIXTURE_DB_PORT=5569
FIXTURE_SWAGGER_PORT=8217
PGDATABASE=warehouse_002
COMPOSE_PROJECT_NAME=wt-002
PGRST_DB_SCHEMA=api_2
